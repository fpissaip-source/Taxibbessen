[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile && npm install -g serve && pnpm --filter @workspace/taxi-app run build"

[deploy]
startCommand = "serve -l $PORT -s artifacts/taxi-app/dist/public"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
